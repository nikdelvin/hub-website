<section class="relative overflow-hidden bg-black pb-32 pt-24">
    <div class="container relative z-10 mx-auto px-4">
        <h2
            class="font-heading mx-auto mb-12 max-w-lg text-center text-6xl font-bold !leading-[1.5] text-white sm:text-8xl"
        >
            Hard to believe?<br />
            <span class="bg-gradient-fuchsia bg-clip-text text-transparent">Get demo UI bundle</span>
        </h2>
        <div class="mx-auto mb-8 md:max-w-lg">
            <div
                class="flex flex-col overflow-hidden rounded-md bg-white p-1 focus-within:ring-4 focus-within:ring-indigo-500 md:flex-row"
            >
                <input
                    class="block flex-1 rounded-bl-xl rounded-tl-xl bg-transparent px-6 py-4 text-base text-gray-500 placeholder-gray-500 outline-none md:py-0"
                    type="text"
                    placeholder="Enter email to get started"
                />
                <button
                    id="email-submit"
                    class="font-heading tracking-px group relative w-full overflow-hidden rounded-md bg-gray-900 px-8 py-4 text-xs font-semibold uppercase text-white md:w-auto"
                >
                    <div
                        class="bg-gradient-fuchsia absolute left-0 top-0 h-full w-full -translate-y-full transform transition duration-500 ease-in-out group-hover:-translate-y-0"
                    >
                    </div>
                    <p class="relative z-10">Try it now</p>
                </button>
            </div>
        </div>
    </div>
</section>
<script>
    import Swal from 'sweetalert2'
    const submit = document.querySelector('#email-submit')
    submit?.addEventListener('click', () => {
        Swal.fire({
            title: 'Registration is temporarily closed',
            width: 345,
            background: '#000 url(/assets/gradients/radial18.svg)',
            customClass: {
                popup: '!bg-black !bg-cover !bg-top border-solid border-[1px] border-gray-800 rounded-2xl',
                title: 'font-heading text-4xl font-bold !leading-[1.5] pb-8 bg-gradient-orange bg-clip-text text-transparent',
                confirmButton:
                    'bg-gradient-orange hover:!bg-gradient-orange-hover pb-4 bg-black transition duration-500 ease-in-out text-white relative overflow-hidden rounded-full px-14 py-5'
            }
        })
    })
</script>
